name: Publish package to pypi

on:
  workflow_dispatch:
  push: 

jobs:

  publish:
    runs-on: ubuntu-latest
    steps:

      - uses: actions/checkout@v2

      - id: get_status
        name: get_status
        uses: ./.github/actions/get_status
        with:
         package_name: lock-defaults

      - name: Show diff results
        if: ${{ steps.tests.outputs.remote_exists == 'true' && steps.get_status.outputs.version_difference == 'true'}}
        shell: bash
        run : |
          echo "this runs"